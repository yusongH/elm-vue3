import { Button, NavBar, Form, Field, CellGroup, Toast, Cell, Icon } from 'vant'

export default app => {
  app.use(Button)
  app.use(NavBar)
  app.use(Form)
  app.use(Field)
  app.use(CellGroup)
  app.use(Cell)
  app.use(Toast)
  app.use(Icon)
}
