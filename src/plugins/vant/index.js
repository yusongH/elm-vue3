import { Button, NavBar, Form, Field, CellGroup, Toast } from 'vant'

export default app => {
  app.use(Button)
  app.use(NavBar)
  app.use(Form)
  app.use(Field)
  app.use(CellGroup)
  app.use(Toast)
}
